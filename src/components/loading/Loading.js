import React from 'react'
import { LoadingContainer, AnimatedLoading } from './LoadingStyles'

function Loading() {
    return (
        <LoadingContainer>
            <AnimatedLoading />
        </LoadingContainer>
    )
}

export default Loading
